const About = ()=>{
    return (
        <div>
            <h1>About Us</h1>
            <p>Namaste React is a React application that showcases a restaurant list with pagination, filtering, and sorting options. It also features a user authentication system using JWT (JSON Web Tokens) and Redux for state management.</p>
        </div>
    );
};

export default About;